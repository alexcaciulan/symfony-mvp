{# Tab Activitate Portal orchestrator. Layout 2+1 col: config + timeline (real)
   în main, how it works + sync status în sidebar. #}

<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

    <div class="lg:col-span-2 space-y-6">
        {{ include('case/overview/_portal_config.html.twig') }}
        {{ include('case/overview/_portal_timeline.html.twig') }}
    </div>

    <aside class="space-y-6">
        {{ include('case/overview/_portal_how_it_works.html.twig') }}
        {{ include('case/overview/_portal_sync_status.html.twig') }}
    </aside>

</div>
